<Window x:Class="VSModManager.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:VSModManager.App"
        mc:Ignorable="d"
        Title="MainWindow"
        Height="400"
        Width="800"
        WindowStartupLocation="CenterScreen"
        >
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        
        <Label Grid.Row="1" Grid.Column="0" HorizontalContentAlignment="Center">Installed Mods</Label>
        <ListBox Grid.Row="1" Grid.Column="0" x:Name="modsList" Width="100" d:ItemsSource="{d:SampleData ItemCount=5}" Margin="0 30 0 5" BorderThickness="0" HorizontalContentAlignment="Center"/>
        
        <StackPanel Grid.Row="1" Grid.Column="1">


        </StackPanel>

    </Grid>
</Window>
